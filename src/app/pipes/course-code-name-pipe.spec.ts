import { CourseCodeNamePipe } from './course-code-name-pipe';

describe('CourseCodeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseCodeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
